# Scope: corrected manager65 WARP action collector v3

## Question

Can the superseded manager65 action-`0x2B` collector be replaced offline with a live-capable read-only collector and independent semantic evaluator that remove the disproven inline-owner state gates, separate the engine viewport from the owned HWND, and remain mechanically unable to promote fixture or self-asserted live data into a WARP activation?

## Included

- canonical `G7MTClient.exe` identity and module-relative root formulas;
- UI-root/registry and inline strategy-owner separation;
- manager65 current WARP-action state and manager67 dormant prior-stage structure;
- double read-only capture, recursive widget/context geometry, action `0x2B` uniqueness, current-scale inverse client region;
- closed semantic evaluation, external source-hash parameters, mutation suite, capability audit, and independent read-only review.

## Excluded

- VM or guest operation, process launch, debugger attach, breakpoint installation, target process access, capture, input, permit issuance;
- server, protocol, database, binary, resource, or VM lifecycle changes;
- stage-gate adapter, live hit-region binding, WARP execution, `0x0B01/0x0B07`, authority, persistence, pixels, Gate-A, or Gate-B.

## Claim ceiling

`OFFLINE_CORRECTED_MANAGER65_ACTION_0X2B_COLLECTOR_V3_PASS_RUNTIME_UNSEEN`.
