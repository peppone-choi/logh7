# Handoff: original-client WARP external live binding subject v1

## Goal and result

Define and mechanically verify the future same-run H1 identity through H6 external binding chain and exact manual WARP hit-region proof without creating a current live receipt, coordinate, binding, permit, or input capability.

- bounded result: `PASS`
- independent review: round 1 `REVISE`, round 2 `APPROVE`
- execution state: `OFFLINE_WARP_EXTERNAL_LIVE_BINDING_SUBJECT_V1_PASS_NOT_CREATED_NOT_ELIGIBLE`
- overall game goal: `INCOMPLETE`

## Scope and actual work

- Created `work/20260829-original-client-warp-external-binding-subject-v1` with a closed Draft 2020-12 schema, contract, evaluator, tests, and verifier.
- Defined H1 identity -> H2 corrected live capture -> H3 evaluation -> H4 independently recomputed hit-region subject -> H5 independent review -> H6 external bundle.
- Kept the current live subject and every H1-H6 step exactly `NOT_CREATED` with null receipt, path, hash, timestamp, rectangle, point, cell, and binding digest.
- Preserved current WARP-only authority as maximum/consumed/remaining `1/0/1`; this contract is not a permit and cannot perform or authorize input.
- Isolated manager65 fixture geometry under `TEST_ONLY_NONPROMOTABLE` and verified raw positive-normal IEEE-754 binary32 scales through exact rational inverse geometry, prescribed midpoint, all nine 3x3 pixels, forward logical replay, and exact 1x1 cell.
- After independent review round 1 found that coordinated alternate geometry could pass, parsed and semantically cross-bound the source capture/evaluation, required externally supplied expected hashes, and added the exact reviewer proof as mutation 115.
- Independent review round 2 reran the verifier and exact coordinated mutation, which was rejected at the command-`0x2B` logical-rectangle source binding. Final verdict: `APPROVE`.
- Performed no live, VM, guest, debugger, target-process, memory, capture, input, permit, server, protocol, database, binary, or resource operation.

## Reproduction

```powershell
pwsh -NoProfile -File work/20260829-original-client-warp-external-binding-subject-v1/verify.ps1
```

Expected result: 4 tests, 115 mutation subtests, Draft 2020-12 PASS, six chain steps not created, test-only vector valid, no geometry escape, authority `1/0/1`, point/digest/permit null, and all state-changing operation counters zero.

## Evidence

- `evidence/warp-external-binding-subject-v1.schema.json`
- `tests/current-offline-contract.json`
- `evidence/current-offline-evaluation.json`
- `evidence/independent-review.json`
- `evidence/artifact-ledger.json`
- `evidence/final-verification.json`
- `report/original-client-warp-external-binding-subject-v1.md`

## Confirmed facts

- A source path and matching SHA-256 do not prove a derived geometry claim; semantic fields must also be parsed and cross-bound.
- The current fixture full client rectangle, unique command `0x2B` logical rectangle, scales, candidate rectangle, safe point, and forward logical point are now bound to the actual manager65 v3 capture/evaluation contents plus externally supplied expected hashes.
- The exact coordinated alternate geometry used in review round 1 is rejected.
- No test coordinate appears in the live evaluation output.
- H1-H6 and the live binding subject remain `NOT_CREATED`; activation point, binding digest, and permit remain null.
- The first readiness blocker remains `FRESH_OWNED_HWND_NOT_OBSERVABLE_FROM_AVAILABLE_GUEST_OPERATION_SESSION`.

## Inference and claim boundary

- The H1-H6 structure is a future live evidence contract, not evidence that any live step exists.
- Exact fixture geometry validates only the offline verifier. It is not a current original-client rectangle or input point.
- Offline PASS and independent approval do not prove WARP, packet transmission, server authority, persistence, player-visible movement, both factions, Gate-A, Gate-B, or playability.

## Unknown and unverified

- Fresh interactive owned HWND, current PID/start/module, listener, heartbeat, foreground, debugger compatibility/attach, and initial per-thread DR state.
- H1 fresh same-run identity and every dependent H2-H6 live artifact.
- A separately issued new WARP permit and the single physical activation.
- Post-WARP SelectGrid creation, destination and confirm authority, outbound `0x0B01`, inbound `0x0B07`, queue completion, server authority, persistence, and player-visible movement.

## Next start

After user direction, create a separate offline `WARP_H1_INTERACTIVE_IDENTITY_ROUTE_V1` unit that resolves the current first blocker without launching the game, attaching a debugger, reading target memory, issuing a permit, or sending input. It must bind the absolute guest executable route, intended interactive session/desktop, unique receipt paths, one-attempt semantics, and fresh owned-HWND identity requirements. Do not execute that route in the contract unit.

## Forbidden retries and promotions

- Do not promote the manager65 fixture rectangle `[560,420,656,436)` or point `(607,427)` into a live subject.
- Do not create H2-H6 before a valid same-run H1 identity and exact predecessor chain.
- Do not let a source-declared hash replace an externally supplied expected hash or let hashes substitute for semantic cross-binding.
- Do not reuse the consumed historical permit, stale PID/HWND, old manager receipt, pointer, rectangle, or point.
- Do not attach, install breakpoints, read target memory, capture, send input, issue a permit, alter VM lifecycle, or change server/protocol/database state from this completed offline unit.
- Do not call this result original-client playability, movement, authority, persistence, both factions, Gate-A, or Gate-B.
