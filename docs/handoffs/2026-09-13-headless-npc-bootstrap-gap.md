# NPC world bootstrap gap — 2026-09-13

## LIVE deployment — supersedes local-only notes below

LATEST superseding1324: server2120 startUTC2026-09-12T21:58:16.6453807Z runs taskRoot/server-fleet-repair-20260913/Logh7.Server.exe, DLLSHA2D8C9F577A510BCD2D7595054AB76023F9FECBF309C10C8A664D8D61D1C3E61A. Schema45->46; full character/unit/fleet state and2supports preserved. Receipts E:/logh7-build/server-fleet-repair-deploy-20260913.json and fleet-repair-postflight-20260913.json (21:58:56.4720245Z) confirm both47900listeners andPG55432. Game5968noTCP, native multiunit repair stillunverified. All older LATEST paragraphs are historical.

LATEST superseding7720: server1324 startUTC2026-09-12T21:16:53.7006786Z runs taskRoot/server-rank-20260913/Logh7.Server.exe, DLLSHA9A72B6E2E5AF67CEDE91722F406A7AD7F28A45E57B7941D000F8A30E84630EA4. Schema45/full character-unit-fleet state/2supports preserved. Receipts E:/logh7-build/server-rank-deploy-20260913.json and rank-postflight-20260913.json (21:17:32.2113257Z) confirm both47900listeners/PG55432connection. Game5968 has noTCP. Cumulative personnel/rank fixes now deployed, native validation still pending. Older LATEST entries below are historical.

LATEST superseding828: server7720 startUTC2026-09-12T20:11:35.9382559Z runs taskRoot/server-personnel-20260913/Logh7.Server.exe, DLLSHA9F894657D8FB72AF91F28C490D601A3DA73F6DFFA9E8D1E62963A3DEC045AC61. Schema45unchanged/full character-unit-fleet state and2supports preserved. Receipts E:/logh7-build/server-personnel-deploy-20260913.json and personnel-postflight-20260913.json (UTC20:12:17.2690181Z); both47900listeners/PGconnected,game5968noTCP. Includes destination completion checks/personnel fixes; no native gameplay claim. Details in strategy-command-current-audit handoff. Following828paragraph is historical.

LATEST:2026-09-12T19:34:40.2838504Z serverPID828 runs taskRoot/server-base-travel-20260913/Logh7.Server.exe, DLLSHA B5DA0A417E3A3DB02613535602DE3C2D1A6567ED930F47BFA3FE56CFE5EF9974. Replaced verified2964, schema44->45. Full character/unit/fleet hashes preserved INCLUDING both support rows, no exclusions. Deployment E:/logh7-build/server-base-travel-deploy-20260913.json; independent base-travel-postflight-20260913.json UTC19:35:15.9530851Z confirms828responding/both47900listeners/PG55432connection. Game5968unchanged/responding/noTCP, NOT native gameplay evidence.

Fresh backup base-travel-backup-20260913.{json,dump}, SHA7CD11860E014E30DA3A02C04F99E8CE07F8DA364B9F4BFBE521519921E0520F7,138256bytes/221entries; restored and candidate process checked in hostDBlogh7_base_travel_restore_20260913, then owned process21244 stopped. ZIP DE28BED6B122791F0A99FF42768237DFD8A2AA89A4B935D0B896EC07D61F8A12. Full1062PASS plus subsequent focused tests support this production candidate. Includes warp actor/unit identity correction, departure survivor correction, base-travel persistence/scheduler/wire handler. BaseTravelDelay remainsNULL default: temporary policy not approved/enabled; no live base-travel action claimed. Detailed newest strategy audit has implementation/test boundaries. Prior binaries/backups retained. Guest deploy script-v2 executed (first copy never run after receipt-name correction).

### Previous deployment (superseded by828)

2026-09-12T18:00:30.5124587Z serverPID2964 now runs guest taskRoot/server-headless-strategy-20260913/Logh7.Server.exe, DLLSHA2568C9FFBB64C88D87F8320C02F8169B5B69F03A7E293CB6CDB5782B7F697C187BF. Replaced verified1260 only after no active game connections and exact previous path/start check. Schema44 unchanged. Existing character/grid-unit and12fleet rows preserved; catalog-defined rear support2113929485/486 registered. Preservation comparison excludes ONLY those2 known absent rows and separately requires their expected kind57/58,grid102,power2,one hull,no damage. Not a wholesale waived fleet-hash check.

Fresh guestbackup E:/logh7-build/headless-backup-20260913.{json,dump}, UTC17:58:06.84469Z, dumpSHA E0F654B1CFC0E26891A32B4F1A1B6A2A1E3A73AE24B4FF6A46471BC73A264C06,138207bytes/221entries. Actually restored to new hostDBlogh7_headless_fresh_restore_20260913 (55439); pg_restore exit0; candidatePID31968 started, zero clients, same surviving defensive/no-AI-event state, then owned cleanup stopped it. Receipt headless-fresh-restore-20260913.json. PackageZIP375033BED817D7C790BFCC4633BA73F2CFE164061468D261C42F6AF6D9F779E2.

Deployment receipt E:/logh7-build/server-headless-strategy-deploy-20260913.json statusSERVER_RUNNING_EXISTING_STATE_PRESERVED_SUPPORT_BOOTSTRAPPED. Independent postflight E:/logh7-build/headless-postflight-20260913.json UTC18:01:16.1632557Z verifies2964responding, owns both47900listeners202.8.80.179/127.0.0.2 and PostgreSQL55432connection. Game5968unchanged/responding/noTCPconnection; no native login or gameplay proof. Full1033test result remains scoped source evidence. Prior binaries/backups/restoreDB retained. Scripts guest-headless-backup,host-headless-fresh-restore-check,guest-server-headless-strategy-deploy,guest-headless-postflight all20260913; deployed script guestfilename has-v2 suffix (first copy not executed after output-path correction).

Next user priority remains strategic formation/reorganization/fullrepair/order lifecycle: docs/handoffs/2026-09-13-strategy-command-current-audit.md. Temporary logistics/stock approval still unanswered. Do not treat the deployed headless functionality as completion of strategic commands or whole game.

## Local correction: viewer-independent NPC profile fields

Restored real-backup candidate verification: verified dump085FF42... and candidateDLL8C9FFBB64C88D87F8320C02F8169B5B69F03A7E293CB6CDB5782B7F697C187BF, created fresh hostDB logh7_headless_restore_20260913 on55439 and pg_restore --exit-on-error --no-owner --no-privileges exited0. Initial host checker failed before server launch because Get-NetTCPConnection is unavailable; kept failed receipt and fixed local listener observation to .NET/netstat. v2 checker exited0, PID54820 owned127.0.0.1:47949, observed5seconds, then exact-path/start-time owned cleanup succeeded. No VM writes/restart. Receipt E:/logh7-build/headless-restore-20260913-v2.json SHA256DCAA29761E9F596FC2E7C275637B1A09B13C9F6983A82CE986A90F1A1B7F0B6C; script host-headless-restore-check-20260913.ps1.

Before hashes match backup; characters/grid-unit hashes and schema44 remain identical. Fleet rows12→14: new catalog-defined rear support units2113929485(kind57)/2113929486(kind58) atgrid102. Existing identities/casualties not rewound. aiEvents0/connections0: offensive outfits2113929568/569 have all4 units destroyed300/300; all surviving catalog outfits are defend, and controller _provoked is process-owned and false at restoration. Thus no forced combat is expected from this saved state; do not label this a real-backup combat PASS. Fresh fixture integration already proves two-sided automatic damage separately. Engagement-latch persistence is an unresolved restart behavior: do not infer provocation from old damage, which existing policy explicitly rejects. No deployment yet, no deletion of backups or test DB, no reset/healing to manufacture a fight.

Full local verification after headless wiring/restart revision guard/signed warehouse exchange:1033PASS/0FAIL/0SKIP,2m24s; session10882 terminal exit0. TRX E:/logh7-build/test-results/headless-strategy-20260913/headless-strategy-prerequisites.trx. Release win-x64 self-contained publish to fresh E:/logh7-build/server-headless-strategy-20260913 exited0. NOT deployed. Next restore-backed candidate verification must account for intentional unattended combat, unlike old exact post-start fleet-hash equality script. Strategy audit handoff contains the current repair/formation gaps and unanswered stock approval.

Restart follow-up: extended actual server integration to stop after reciprocal damage, save one undamaged survivor at grid105/X123/Y45 and one fully destroyed unit at grid106, then start a fresh server instance with no connections. RED: grid105 empty after restart. Cause: ReconcileUntouchedTemplateAsync documented first-revision-only behavior but SQL omitted revision guard, moving previously saved undamaged units back to authored grid. Added AND revision=1. Related Fleet/Controller/headless72PASS/0FAIL/0SKIP (session75230 exit0). Restart asserts exact saved records with expected revision, moved grid registration/X123 and destroyed unit registration. Scenario writes are isolated test setup, not player-issued warp proof. Local-only, no VM deployment. User then requested strategy-command audit; continue from that priority.

Startup wiring implemented locally: NaturalAuthorityServer loads one configured catalog (optional constructor injection), shares it with all sessions, unions nonzero authored ship grids and persisted occupied grids, acquires each normal tactical grid lease and calls the common restorer BEFORE starting listeners/timer. Authored commander frames use account-independent profiles and zero public location/balances, matching the inspected NPC branch of EncodeLocatedCharacter. No fake player session is constructed. Initial lease used subordinate complement and hit TACTICAL_BATTLE_TEMPLATE_CHANGED; corrected to the same TacticalShipCapabilities.Number used by normal scene entry.

New OriginalHeadlessBootstrapTests uses isolated PostgreSQL and actual NaturalAuthorityServer with fleet-skirmish configured, no client connections. RED confirmed zero restored fleet rows before wiring. GREEN source-level regression run110PASS/0FAIL/0SKIP (session27884 exit0). Then strengthened integration assertion polls actual DB until two distinct powers have damaged units and checks npc-ai receipt and absence of connection-accepted; this passed in6seconds (subsequent test process exit0). This proves unattended startup and durable reciprocal NPC combat in the test fixture, not native visual battle or restart fidelity. Next: persisted moved-grid/dead-unit/restart tests, full suite, safe backup/restore/deploy validation. Production1260 remains unchanged; no deployment in this turn.

Shared restoration extraction implemented: OriginalFleetRosterRestorer.RestoreAsync takes store, registry, catalog, target grid, arms and commander-frame callback, with optional own-corps override. Moved the existing seed/reconcile/validate/controller-profile/register/casualty/binding sequence there; session retains injury/provider guard and own-corps restore, then delegates. Preserves existing scene-refresh behavior and grid lease requirement. Existing Fleet/Controller/NPC-character suites with isolatedPG80PASS/0FAIL/0SKIP, session12397 exited0. This is a refactor of tested restoration, not yet headless startup wiring. Startup must provide independent commander encoding, enumerate stored+authored grids and own the leases. Local-only; production1260 unchanged.

Stored-controller projection extraction: moved existing validated ProjectStoredCharacter body unchanged into OriginalStoredCharacterProjection.Create(CharacterReadRecord), leaving a session forwarding method. Preserves byte/rank/ability range rejection, fresh copied abilities, nullable hull compatibility projection and merit/return-base fields; accepts no session or mutable balance state. This is a refactor of existing behavior, not new biography defaults. Character/Controller filtered suite with isolated PostgreSQL57PASS/0FAIL/0SKIP, session12992 completed. Shared fleet restorer can now use authored and persisted profile projectors without constructing a session. Startup bootstrap still not wired, all these changes local-only.

Storage prerequisite implemented: PostgresFleetUnitStore.ReadOccupiedGridsAsync queries distinct ordered grid_id from all persisted fleet rows (including defeated units, whose casualties must not be lost). Isolated-PG test first failed for absent method, then passed empty/one-grid/moved-grid/deduplicated-two-grid assertions alongside existing casualty/revision preservation. Post-green test switched reflection discovery to direct typed call. No production DB writes by this read API and no deployment. Startup union/restore still needs wiring.

Refactor follow-up: moved neutral defaults and fleet commander construction into public account-independent `OriginalAuthoredNpcProfiles` in OriginalGateway. Session CommanderFrame now delegates profile construction there and retains location encoding; legacy enemy uses the same neutral defaults. This extraction preserves the preceding tested behavior (not a new policy). Related character/range22PASS. Server-startup restoration can now construct the same authored fleet profiles without fabricating a NaturalAuthoritySession. No bootstrap wiring or deployment yet.

Follow-up: removed CommanderFrame's `_createdCharacter` guard and the unused viewer argument from CreateBattlefieldCommanderCharacter. A test generates the named fleet frame, clears the session's created character, then requires identical nonnull bytes; failed before guard removal and passes afterward. Character-merit class now10PASS. This removes the frame-generation login prerequisite but the method still belongs to the session and uses its location encoding; no server-startup fleet restoration is connected yet. Local-only, not deployed1260.

Strengthened OriginalNpcCharacterMeritTests to vary viewer portrait/abilities as well as rank/merit and compare the entire decoded NPC0323 frame across viewers. Initial fixture uint/int type mistake corrected; then all4 primary/fleet and default/configured variants failed because NPC frames differed. Replaced viewer `source with` bases in both NPC character constructors with fresh authored neutral defaults (zero/empty unspecified profile fields, fresh8-byte abilities, empty raw payload); content still supplies name, side, rank, merit and hull. All9 class tests pass. Neutral values are authored placeholders, not recovered original biographies or balance. No deployment/full-suite run yet. The source parameter and CommanderFrame's viewer guard still exist and must be removed when extracting the account-independent projector; this fix alone does NOT implement headless bootstrap.

Fresh source inspection after overlap deployment finds a whole-game gap distinct from controller correctness.

- NaturalAuthorityServer.StartAsync starts its own250ms RunNpcLoopAsync; player input does not drive this timer.
- Its `_battles` is constructed empty. AdvanceNpcsAsync only iterates registered battles/NPCs.
- Production RegisterNpc call sites are in NaturalAuthoritySession.RegisterAuthoredNpc and NaturalAuthoritySession.FleetPersistence restoration.
- ResetBattleObservationForSceneImportAsync invokes RestoreFleetRosterAsync and RegisterAuthoredNpc during a player's scene import. NaturalAuthorityServer creates sessions from HandleConnectionAsync, not startup.
- Therefore a fresh process with no player scene import has a running timer but no populated NPC world in this recovered path. NPCvsNPC registry tests manually register units and do not prove unattended restart simulation. Existing postflight's unchanged DB and no game connections must not be interpreted as proof NPC battles are progressing.

This is a verified source-level bootstrap dependency, not merely a missing test. Existing previously-imported grids may continue after disconnect; do not claim all NPC simulation requires a currently connected player.

## Dependency split verified from current source

Catalog/startup follow-up: NaturalAuthoritySession currently owns a process-static Lazy that reads LOGH7_BATTLEFIELD_CATALOG on first use; NaturalAuthorityServerCommand does not load/pass a catalog and NaturalAuthorityServer's connection factory omits the optional battlefieldCatalog argument. The startup seam is after migrations/store creation and before StartAsync accepts connections. Load/inject one instance there and pass the same instance to sessions; retain optional/default behavior for existing test callers.

Grid discovery must not rely only on AuthoredShips grid IDs: PostgresFleetUnitStore currently exposes ReadGridAsync but no distinct stored-grid enumeration. Persisted units can occupy different grids after gameplay. Startup should union explicit authored grids and distinct persisted grid IDs, then apply existing ProjectFleetUnit(row.UnitId,row.GridId)/validation rules. Otherwise unattended restored fleets on moved grids would silently never tick. Missing catalogs must remain errors, not deletes or reseeds. This discovery added no code and did not change running1260.

NaturalAuthoritySession.FleetPersistence.cs mixes: player corps restore; template seeding/reconciliation; saved row/catalog validation; unavailable-controller release; public controller profile lookup; NPC registration; casualty restore and durable-store binding. Headless initialization must share the latter operations, not clone only RegisterNpc and lose persistence. Existing live NPC refresh updates public character data without replacing pose/damage: retain that distinction for later player entry.

Additional account coupling: CommanderFrame returns null unless `_createdCharacter` exists, and CreateBattlefieldCommanderCharacter builds the NPC profile with `source with {...}` from the viewer. Several unspecified profile fields therefore remain viewer-derived. NaturalAuthorityServer currently has no explicit battlefield catalog field/injection, while session ActiveBattlefieldCatalog resolves its private injected/default catalog. Startup must use the SAME configured catalog, not separately load the default catalog. IOriginalFleetUnitStoreProvider exposes concrete PostgresFleetUnitStore; stored controller resolution must retain ReadControllerCharacterAsync and side/generation checks.

Implementation sequence: introduce account-independent authored commander projection with explicit authored/default fields and tests (no viewer base record); extract shared per-grid fleet restore with optional own-corps override for session refresh; load configured catalog once for server startup/session consistency; enumerate authored grid IDs without treating fallback grid0 as an actual grid; bootstrap before NPC loop/acceptance; then verify no-client persisted battle and restart. Do not make a fake login to reuse private session methods. Policy for unknown/unlisted persisted units remains fail-closed as current FLEET_UNIT_CATALOG_MISSING, not silent removal. No source changes in this dependency inspection.

Next implementation: separate persisted fleet restoration/registration from account-specific scene import; bootstrap authored/persisted active grids at server startup with the same generation/revision/controller/outfit checks, public commander projection and defensive posture. Do not create a fabricated player session or seed/reset existing rows to start simulation. Add real NaturalAuthorityServer startup test with zero client connections, durable NPC fleets on both sides, movement/damage persisted, then restart verification. Inspect catalog/provider APIs and existing restoration helpers first; retain world-entry projection separately. No implementation or new deployment in this discovery unit. Full goal ACTIVE.
